syntax = "proto2";

package space_coop.server.core;

message NetworkPlayer {
  optional string player_key = 1;
  optional string player_ip = 2;
  optional bool connected = 3;
}

message PlayerData {
  repeated NetworkPlayer players = 1;
}
