syntax = "proto2";

package common.pubsub;

message PubSubConfig {
  repeated string listens_to = 1;
  repeated string posts_to = 2;
}

message PubSubMessage {
  optional string source = 1;
  optional bytes payload = 2;
}
